<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin - Diff Daily" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <nav class="bg-gray-800 text-white">
      <div class="container mx-auto px-6 py-4 flex justify-between items-center">
        <div class="flex items-center space-x-6">
          <h1 class="text-xl font-bold">
            <%= link_to "DiffDaily Admin", admin_root_path, class: "hover:text-gray-300 transition" %>
          </h1>
          <% if admin_signed_in? %>
            <div class="flex space-x-4 text-sm">
              <%= link_to "Dashboard", admin_root_path, class: "hover:text-gray-300" %>
              <%= link_to "Repositories", admin_repositories_path, class: "hover:text-gray-300" %>
            </div>
          <% end %>
        </div>

        <% if admin_signed_in? %>
          <div class="flex items-center space-x-4">
            <% if current_admin_user["avatar_url"].present? %>
              <img src="<%= current_admin_user["avatar_url"] %>" alt="Avatar" class="w-8 h-8 rounded-full">
            <% end %>
            <span class="text-sm"><%= current_admin_user["name"] %></span>
            <%= button_to "Logout", admin_logout_path, method: :delete, class: "text-sm bg-gray-700 hover:bg-gray-600 px-3 py-1 rounded" %>
          </div>
        <% end %>
      </div>
    </nav>

    <main class="container mx-auto px-6 py-8">
      <% if flash[:notice] %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-6">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
